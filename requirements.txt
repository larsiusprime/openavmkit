azure-storage-blob==12.29.0
catboost==1.2.8
numpy==2.3.5
huggingface-hub==1.17.0
lightgbm==4.6.0
markdown==3.10.2
mgwr==2.2.1
ngboost==0.5.10
optuna==4.6.0
osmnx==2.1.0
paramiko==4.0.0
pdfkit==1.0.0
pipreqs==0.5.0
polars==1.38.1
pyarrow==22.0.0
pytest==9.0.2
python-dotenv==1.2.1
rich==15.0.0
seaborn==0.13.2
statsmodels==0.14.6
xgboost==3.2.0
xlsxwriter==3.2.9
census==0.8.26
# layeredcompmodel 0.3.0 adds the JSON model cache (to_dict/from_dict) + vectorized predict, which
# `run_layeredcomp` uses to skip the entire lcomp fit on unchanged training data. We stay on the PyPI
# 0.2.1 pin until 0.3.0 is released: pyproject.toml feeds this file straight into the package
# metadata, and PyPI rejects direct-URL (git+https) dependencies, so a fork pin here would block
# `twine upload`. The code guards on capability (`hasattr(to_dict/from_dict)`), not version, so 0.2.1
# simply falls back to a full fit — correct, just slower.
# To opt into the fork locally (enables the cache + the two lcomp serialization tests):
#   pip install "layeredcompmodel @ git+https://github.com/larsiusprime/LayeredCompModel.git@feature/perf-vectorize-and-parallel"
# When 0.3.0 lands on PyPI, bump this pin to `==0.3.0` and delete this comment block.
layeredcompmodel==0.2.1
shap==0.50.0
numba==0.65.1
mpld3==0.5.12
hilbertcurve==2.0.5
optuna-integration==4.6.0

tabulate~=0.9.0
pygam~=0.12.0
pandas~=2.3.3
ipython~=8.12.3
geopandas~=1.1.2
matplotlib~=3.10.9
pyproj~=3.7.2
shapely~=2.1.2
azure-core~=1.38.1
requests~=2.34.2
joblib~=1.5.3
scipy>=1.11.4,<1.17
scikit-learn~=1.8.0
tqdm~=4.67.3
rasterio~=1.4.0
seamless-3dep>=0.4.1,<0.6